interface:
  display_name: "Manim Video"
  short_description: "Generate and render Manim explainer videos."
  default_prompt: "Use $manim-video to create a short Manim explainer video."

policy:
  allow_implicit_invocation: true
